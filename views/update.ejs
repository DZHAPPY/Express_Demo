<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <h1>修改学生信息</h1>
    <form action="/students/update-student" method="post">
        <input type="hidden" name="id" value="<%=student.id%>">
        <div><input type="text" name="name" value="<%=student.name%>"></div>
        <div><input type="number" name="age" value="<%=student.age%>"></div>
        <div>
            <input type="radio" name="gender" value="男" <%=student.gender === '男' && 'checked'%>>男
            <input type="radio" name="gender" value="女" <%=student.gender === '女' && 'checked'%>>女
        </div>
        <div><input type="text" name="address" value="<%=student.address%>"></div>
        <button>修改</button>
    </form>
</body>
</html>